import 'package:flutter/material.dart';

class DarkThemeColors {
  static const primaryColor = Color(0xFFFFFFFF); // White primary color
  static const backgroundColor = Color(0xFF000000); // Black background
  static const surfaceColor = Color(0xFF000000); // Black surface
  static const textColor = Color(0xFFFFFFFF); // White text color
  static const accentColor = Color(0xFFFFFFFF); // White accent color
  
  // Additional colors for authentication screen
  static const authBackground = Color(0xFF000000);
  static const authTextColor = Color(0xFFFFFFFF);

  // Additional colors for user's list
  static const userListBackground = Color(0xFF000000);
  static const userListTextColor = Color(0xFFFFFFFF);
  static const userListDividerColor = Color(0xFF444444);
}
