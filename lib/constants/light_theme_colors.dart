import 'package:flutter/material.dart';

class LightThemeColors {
  static const primaryColor = Color(0xFF000000); // Black primary color
  static const backgroundColor = Color(0xFFFFFFFF); // White background
  static const surfaceColor = Color(0xFFFFFFFF); // White surface
  static const textColor = Color(0xFF000000); // Black text color
  static const accentColor = Color(0xFF000000); // Black accent color
  
  // Additional colors for authentication screen
  static const authBackground = Color(0xFFFFFFFF);
  static const authTextColor = Color(0xFF000000);

  // Additional colors for user's list
  static const userListBackground = Color(0xFFFFFFFF);
  static const userListTextColor = Color(0xFF000000);
  static const userListDividerColor = Color(0xFFBDBDBD);
}
